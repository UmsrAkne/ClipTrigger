<Window
    x:Class="ClipTrigger.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:ClipTrigger.Models"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewModels="clr-namespace:ClipTrigger.ViewModels"
    Title="{Binding Title}"
    Width="600"
    Height="480"
    d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Source directory path:" />
            <TextBox
                Margin="0,5"
                Padding="1"
                Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" />

            <Button
                Margin="0,0,0,5"
                Padding="20,2"
                HorizontalAlignment="Right"
                Command="{Binding AddSourceDirectoryCommand}"
                Content="Add" />
        </StackPanel>

        <ListBox
            Grid.Row="1"
            AlternationCount="2"
            ItemsSource="{Binding SourceDirectories}">

            <!-- 追加: キー操作の割り当て -->
            <ListBox.InputBindings>
                <KeyBinding
                    Key="Space"
                    Command="{Binding ToggleIncludeInSearchCommand}"
                    CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListBox}}" />
            </ListBox.InputBindings>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="1" />
                    <Setter Property="Margin" Value="1" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="Template" Value="{StaticResource ListBoxItemSimpleTemplate}" />
                    <Style.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter Property="Background" Value="AntiqueWhite" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightSalmon" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <CheckBox
                            Grid.Column="0"
                            Margin="3,0"
                            IsChecked="{Binding IncludeInSearch}" />

                        <TextBlock
                            Grid.Column="1"
                            Margin="3,0"
                            Text="{Binding FullPath}" />

                        <Button
                            Grid.Column="2"
                            Margin="5,0"
                            Padding="8,1"
                            Command="{Binding DataContext.RemoveSourceDirectoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"
                            Content="削除" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel
            Grid.Row="2"
            Margin="0,8,0,4"
            Orientation="Horizontal">
            <Border>
                <TextBlock Text="playback history:" />
            </Border>
        </StackPanel>

        <ListBox
            Grid.Row="3"
            AlternationCount="2"
            ItemsSource="{Binding PlayHistory}">

            <ListBox.InputBindings>
                <KeyBinding
                    Key="Enter"
                    Command="{Binding PlayFromHistoryCommand}"
                    CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListBox}}" />
            </ListBox.InputBindings>

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding PlayFromHistoryCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=SelectedItem}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="1" />
                    <Setter Property="Margin" Value="1" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="Template" Value="{StaticResource ListBoxItemSimpleTemplate}" />
                    <Style.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter Property="Background" Value="AntiqueWhite" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightSalmon" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:PlaybackHistoryItem}">
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="3,0"
                            Text="{Binding FileName}" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="3,0"
                            Text="{Binding FullPath}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>